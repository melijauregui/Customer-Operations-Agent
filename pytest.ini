[pytest]
asyncio_mode = auto
pythonpath = .
addopts = -m "not integration"
markers =
    integration: tests that hit the real OpenAI API (cuestan tokens, requieren OPENAI_API_KEY). Correr con: pytest -m integration
